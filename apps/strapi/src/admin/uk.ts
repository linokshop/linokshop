/**
 * Point fixes for strings Strapi's own Ukrainian translation doesn't cover.
 * Keep this file to genuine gaps found live in the admin — not a full
 * translation (see MEMORY on why we dropped the old cs.ts full override).
 */
export const uk = {
  // i18n plugin: the "AVAILABLE IN" column header on localized list views.
  "i18n.list-view.table.header.label": "Доступно в",
  // content-manager plugin: the "STATUS" column header / filter label.
  "content-manager.containers.list.filters.status": "Статус",
}
